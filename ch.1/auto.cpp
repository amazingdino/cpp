#include <iostream>

int main(void)
{
    auto b = true; // bool
    auto ch = 'x'; // char
    auto i = 123; // int
    auto d = 1.2; // double
    auto z = sqrt(y); // type of whatever the sqrt(y) returns
    auto bb{true}; //bool

    // not many reason but big takeaways here is
    // when the type is very long
    // or
    // programmer might exactly not know which type it requires
    
    return 0;
}
